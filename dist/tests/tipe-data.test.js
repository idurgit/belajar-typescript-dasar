"use strict";
describe('Data Type', function () {
    it("should must declare", function () {
        const name = "Jaka Kelana Umbara";
        const balace = 100;
        const isVip = true;
        console.info(name);
        console.info(balace);
        console.info(isVip);
    });
});
